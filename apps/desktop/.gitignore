# Only what is specific to this app. Everything shared — node_modules, dist,
# logs, editor and OS files, caches — is covered by the root .gitignore, and
# repeating it here meant the two could drift apart.

# Vite's convention for a file that must never leave the machine.
*.local
