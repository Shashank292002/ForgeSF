# Cargo's build directory.
/target/

# Tauri regenerates these capability and ACL schemas on every build, so they
# are output, not source. They used to be committed *and* listed here, which
# git resolves in favour of tracking — so every build showed them as modified.
/gen/schemas/
